{% extends "GloryCategoryBundle::layout.html.twig" %}

{% block content %}
    <div class="box">
        <div class="box-body">
            <form action="{{path('glory_category_create')}}" method="post">
                {{form_widget(form)}}
                <button type="submit" class="btn btn-default">Create</button>
            </form>
        </div>
    </div>
{% endblock %}
